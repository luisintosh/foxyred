{
  "name": "foxyred",
  "description": "Shorten URLs",
  "keywords": [
    "dokku",
    "rails"
  ],
  "scripts": {
    "dokku": {
      "postdeploy": "bundle exec rake db:migrate && bundle exec whenever"
    }
  }
}