<%= form_for(payout_rate) do |f| %>
  <% if payout_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payout_rate.errors.count, "error") %> prohibited this payout_rate from being saved:</h2>

      <ul>
      <% payout_rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :country %>
    <%= f.text_field :country %>
  </div>

  <div class="field">
    <%= f.label :country_code %>
    <%= f.text_field :country_code %>
  </div>

  <div class="field">
    <%= f.label :earn %>
    <%= f.text_field :earn %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
